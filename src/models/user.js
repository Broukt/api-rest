class User {
    constructor(uuid, name, email, last_names, password, is_deleted) {
        this.uuid = uuid;
        this.name = name;
        this.email = email;
        this.last_names = last_names;
        this.password = password;
        this.is_deleted = is_deleted;
    }
}